@model Landing

@{
    ViewData["Title"] = "Удаление посадки";
}

<h1>Удаление посадки</h1>

<h3>Вы уверены, что хотите удалить эту посадку?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-3">
            Рейс
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.Flight.FlightNumber)
        </dd>
        <dt class="col-sm-3">
            Место посадки
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.Location)
        </dd>
        <dt class="col-sm-3">
            Время посадки
        </dt>
        <dd class="col-sm-9">
            @Model.Time.ToString("dd.MM.yyyy HH:mm")
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a asp-action="Index" asp-route-flightId="@Model.FlightId" class="btn btn-secondary">Назад к списку</a>
    </form>
</div> 